import { SET, RESET } from '../actions/user'

const initialState = []

export default function contentPages(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
